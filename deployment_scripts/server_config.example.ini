[django]
secret_key = {django-scret-key}
host = app.example.com
admin_host = p.example.com
static_root = /var/lib/django/static/

[caddy]
host = example.com

[email]
host_password = support_password
host = smtp.zoho.com
host_user = support@example.com

[database]
password = {database_password}
name = {database_name}
user = {database_username}

[sentry]
dsn = {sentry_dns}

[github]
url = git@github.com:{owner}/{repo}.git
name = {repo}
token = {github_pat}
artifacts_url = https://api.github.com/repos/{owner}/{repo}/actions/artifacts

[discord]
url = https://discord.com/api/webhooks/{id}/{token}